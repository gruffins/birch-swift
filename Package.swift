// swift-tools-version: 5.6

import PackageDescription

let package = Package(
    name: "Birch",
    products: [
        .library(
            name: "Birch",
            targets: ["Birch"])
    ],
    dependencies: [],
    targets: [
        .target(
            name: "Birch",
            dependencies: []),
    ]
)
